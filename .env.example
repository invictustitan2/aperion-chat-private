# Google Cloud configuration (Speech-to-Text / Text-to-Speech)
GOOGLE_CLOUD_PROJECT_ID=your-project-id

# Local Node usage (path to a service account JSON key)
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/key.json

# Cloudflare Worker usage (recommended): store the service account JSON as a secret string
# GOOGLE_APPLICATION_CREDENTIALS_JSON='{"type":"service_account",...}'

# Google AI Studio (Gemini) API key for LLM replies
GEMINI_API_KEY=your-gemini-api-key
GEMINI_MODEL=gemini-1.5-flash
