# Aperion Chat - Environment Variables Template
# Copy this file to .env and fill in the values

# ============================================================================
# API Configuration
# ============================================================================

# URL of the API Worker
# Local: http://127.0.0.1:8787
# Production (browser contract): /api (same-origin via https://chat.aperion.cc/api/*)
# Tooling/back-compat: https://api.aperion.cc
VITE_API_BASE_URL=http://127.0.0.1:8787

# ============================================================================
# Authentication
# ============================================================================

# Optional: legacy token auth (API-only dev/test)
#
# Notes:
# - The web UI does not use a client-baked token; VITE_AUTH_TOKEN is intentionally treated as invalid.
# - For API-only local testing in token/hybrid mode, set AUTH_TOKEN and ensure the Worker has API_TOKEN set.
#
# AUTH_TOKEN=your-secure-token-here

# ============================================================================
# Cloudflare Configuration (for deployment scripts)
# ============================================================================

# Cloudflare API Token (for CI/CD and local deployments)
# Get from: https://dash.cloudflare.com/profile/api-tokens
# Required permissions: Workers, Pages, D1, Vectorize
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token

# Cloudflare Account ID
# Find in: Cloudflare Dashboard → Workers & Pages → Overview
CLOUDFLARE_ACCOUNT_ID=your-account-id

# ============================================================================
# Optional: Google Cloud Services (for TTS/STT fallback)
# ============================================================================

# Google Cloud Project ID
# GOOGLE_CLOUD_PROJECT_ID=your-project-id

# Google Application Credentials (JSON as string)
# GOOGLE_APPLICATION_CREDENTIALS_JSON='{"type":"service_account",...}'

# ============================================================================
# Optional: Gemini AI (alternative to Workers AI)
# ============================================================================

# Gemini API Key
# Get from: https://makersuite.google.com/app/apikey
# GEMINI_API_KEY=your-gemini-api-key

# Gemini Model (default: gemini-pro)
# GEMINI_MODEL=gemini-pro

# ============================================================================
# Development Notes
# ============================================================================
#
# 1. After creating .env, restart your dev servers:
#    - pnpm -C apps/api-worker dev:api
#    - pnpm -C apps/web dev
# 2. Verify setup: ./scripts/verify-auth-setup.sh
# 3. For production deployment, set these in Cloudflare Pages settings
# 4. See docs/authentication-setup.md for detailed instructions
