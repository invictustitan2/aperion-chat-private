import { afterEach, beforeEach } from "vitest";

beforeEach(() => {
  // Initialize any necessary mocks or global configurations here
});

afterEach(() => {
  // Clean up after each test if necessary
});
